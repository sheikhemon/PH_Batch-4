/* To lowercase and Uppercase */

const userName = 'Emon SHeIkh';
const updateUserName = 'Emon Sheikh';
if ( userName.toLowerCase() == updateUserName.toLowerCase() ) {
    console.log(`user exists`);
}